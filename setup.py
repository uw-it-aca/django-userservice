#!/usr/bin/env python

from distutils.core import setup

setup(name='Django-UserService',
      version='1.0',
      description='Django user impersonation',
      install_requires=['Django==1.4'],
     )
